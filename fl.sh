#!/usr/bin/env sh

fl () {
  echo "fl"
  clear
  # path=$(cat $HOME/.config/zsh/fl | awk 'BEGIN { FS = " " } { print $2 }' | gum filter)
  # echo $path;
  # exa --icons -la --color=always --group-directories-first
  # cd - || return
}
